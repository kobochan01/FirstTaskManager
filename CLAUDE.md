# FirstTaskManager — Claude Code プロジェクトルール

## Issueラベル定義

| ラベル | 用途 |
|---|---|
| `feature` | 新機能の追加 |
| `bug` | 不具合の修正 |
| `docs` | ドキュメントの変更 |
| `chore` | ビルド・設定・依存関係の変更 |
| `question` | 調査・検討事項 |

---

## 技術スタック

| 役割 | 技術 | バージョン |
|------|------|-----------|
| フロントエンド | React + TypeScript | React 19.2.6 / TypeScript 6.0.3 |
| フロントエンドビルド | Vite | 8.0.13 |
| ルーティング | React Router DOM | 7.15.1 |
| バックエンド | Java + Spring Boot | Java 21 / Spring Boot 3.3.5 |
| バックエンドビルド | Maven (mvnw) | 3.9.15 |
| ORM | Spring Data JPA + Hibernate | Spring Boot 3.3.5 同梱 |
| データベース | PostgreSQL | 16 |
| コンテナ | Docker | 29.4.3 |
| コンテナ管理 | Docker Compose | 5.1.3 |

## 開発環境起動

```bash
# DB起動
docker-compose up -d

# バックエンド起動
cd backend && ./mvnw spring-boot:run

# フロントエンド起動
cd frontend && npm run dev
```

## ポート管理（必須ルール）

以下のポートはプロジェクトで固定されており、**変更してはならない**。

| サービス | ポート |
|---|---|
| バックエンド（Spring Boot） | `8080` |
| フロントエンド（Vite / React） | `5173` |
| データベース（PostgreSQL） | `5432` |

### ポート競合時の対処ルール

1. **別ポートで起動することを禁止する**
   - `--port 3000` や `server.port=8081` など、代替ポートを使った一時起動は行わない
   - `vite.config.ts` のプロキシ設定と CORS 設定がデフォルトポートを前提としているため、別ポートでは正しく動作しない

2. **競合プロセスを停止してから起動する**
   - ポート競合が発生した場合は、そのポートを使用しているプロセスを特定して停止する
   - Windows での確認・停止コマンド：
     ```powershell
     # ポートを使用しているPIDを確認
     netstat -ano | findstr :<ポート番号>
     # プロセスを停止
     taskkill /PID <PID> /F
     ```

3. **Claude Codeの行動規範**
   - サーバー起動前に必ず当該ポートの空き状況を確認する
   - 競合を検出した場合はユーザーに報告し、競合プロセスを停止してからデフォルトポートで起動する
   - ユーザーから「別のポートで起動して」と指示された場合も、このルールを説明して断る

---

## 品質チェックルール

### 実装前の確認
- 既存の類似コンポーネント・関数・パターンを先に探し、再利用を優先する
- DBスキーマ変更を伴う場合は、事前にユーザーへ報告・確認する
- 複数ファイルにわたる変更の場合は、実装前に変更方針をユーザーに説明する

### フロントエンド品質（React + TypeScript）
- TypeScript の型エラーが 0 件であること（`tsc --noEmit` でチェック）
- `any` 型の使用を禁止する（やむを得ない場合はコメントで理由を明記）
- ユーザー入力は API 送信前に必ずバリデーションを実施する
- API レスポンスのエラーハンドリングを必ず実装する（`.catch` または `try-catch`）
- null/undefined が起こりうる箇所はオプショナルチェイン（`?.`）を使う

### バックエンド品質（Java + Spring Boot）
- 複数テーブルへの書き込みメソッドには必ず `@Transactional` を付与する
- コントローラーのリクエストボディには `@Valid` + Bean Validation アノテーションを付与する
- N+1 クエリが発生していないこと（JPA の fetch 設定・クエリ数を確認する）
- 例外は適切にハンドリングし、クライアントに意味のあるエラーレスポンスを返す

### テスト（TDD）
- コードを追加・修正するたびに、対応するテストを必ず書く
- テスト実行後、全テストがパスすることを確認してから作業完了とする
- テストが失敗した場合は原因を特定し、パスするまで試行錯誤する
- **フロントエンドのテスト実行**: `cd frontend && npm test`
- **バックエンドのテスト実行**: `cd backend && ./mvnw test`
- フロントエンド: Vitest でコンポーネント・ユーティリティ関数のテストを書く
- バックエンド: JUnit 5 + Spring Boot Test でサービス層・リポジトリ層のテストを書く

### セキュリティ・エラー防止
- 環境変数（シークレット・DB パスワード等）をコードにハードコードしない
- SQL は JPA / JPQL を使い、生の文字列結合による SQL 構築を禁止する
- フロントエンドでユーザー入力を `RegExp` コンストラクタに渡す際は必ずエスケープする
